import { NativeEventEmitter, NativeModules } from 'react-native';
export const FileAccessEventEmitter = new NativeEventEmitter(NativeModules.RNFileAccess);

/**
 * Native module API.
 *
 * Most functions are the same as the JS wrapper. However native calls do
 * not support default parameters.
 */
export const FileAccessNative = NativeModules.RNFileAccess;
//# sourceMappingURL=native.js.map